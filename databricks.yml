bundle:
  name: gg-renewable-operations-demo

include:
  - resources/*.yml

sync:
  exclude:
    - .venv
    - .git
    - evidence
    - presentation/*.pptx
    - presentation/rendered
    - tests

targets:
  dev:
    default: true
